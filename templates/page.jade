// page.jade -- last stop before rendering.

include(util)



append head
  +css(page.metadata.style)
  +js(page.metadata.logic + '.js')


append content
    header.white
      if (locals.owner)
        h4.quarter-top-space
          span.quarter-left-space
            if (locals.ownurl)
              a(href=locals.ownurl)=locals.owner
            else
              | !{locals.owner}
          span.logo
            if (locals.icon)
              - var iconClass = 'icon-' + locals.icon;
              | &nbsp;
              i(class=iconClass)
              | &nbsp;
            | !{locals.name}
          | : &nbsp;
          span.title
            if (page.metadata.icon)
              - var i = 'icon-' + page.metadata.icon;
              i(class=i)
              | &nbsp;
            | !{page.title}
          a.push-right(href='/help.html')
            i.icon-question-sign
          span.beta.push-right.small
            | BETA &nbsp;
      include navigation


    main.white
      | !{page.html}

    footer.footer
      span.small.push-right
        | Unauthorized use of this system is strictly forbidden and subject to prosecution under PIPEDA.
      span.small.push-left
        | © 2013 MedExtra. All Rights Reserved.

extends site
